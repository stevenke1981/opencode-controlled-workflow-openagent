Record a durable project memory.

Use `memory_add` with the right type:

- success: verified fix
- failure: failed attempt
- pattern: reusable rule
- decision: chosen tradeoff
- research: external/source/MCP/skill finding
- note: short searchable note

Include exact commands, error text, changed files, verification evidence, and tags. Redact secrets.
