Run the memory-first workflow.

1. Use `memory_search` for similar past problems.
2. Use `memory_read` for the best match.
3. Check success records before trying new fixes.
4. Check failure records to avoid repeated mistakes.
5. Try one solution at a time.
6. Verify.
7. Save the result with `memory_add`.
