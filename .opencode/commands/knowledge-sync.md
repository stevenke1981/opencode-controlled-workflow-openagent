---
description: Organize memory ledgers into a searchable solution index for future tasks.
agent: librarian
---

Run `memory_search` across all types to find prior relevant entries. Use `memory_add type=note` to create a searchable index with tags, contexts, and links to the most useful entries. Keep entries concise and append-only unless fixing obvious duplication.
