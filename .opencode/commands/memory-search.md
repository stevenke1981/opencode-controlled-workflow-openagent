Search project memory before attempting a solution.

Call `memory_search` using:

- exact error text
- command name
- package/library/framework
- file path
- OS/toolchain keywords
- tags such as windows, cmake, rust, opencode, mcp

Then use `memory_read` on the best result before editing code.
